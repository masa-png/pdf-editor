# PDF編集ソフト 要件定義書

## 1. システム概要

### 1.1 システム名
PDF Editor Desktop

### 1.2 目的
MacBook上でPDFファイルを開き、PDF内に存在する既存文章を選択し、文章そのものを直接書き換えられるデスクトップアプリケーションを開発する。

特に以下を最重要機能とする。

- PDF内の既存テキストを選択できる
- 選択した文章を編集できる
- 元の文章をPDFから削除できる
- 編集後の文章を同じ位置へ再配置できる
- 編集済みPDFを保存できる

---

# 2. 技術構成

## 2.1 フロントエンド

以下を使用する。

- Tauri 2
- React
- TypeScript
- PDF.js

### 役割

フロントエンドでは以下を担当する。

- PDF表示
- ページ移動
- ズーム
- テキスト選択
- テキスト編集UI
- 編集対象領域の表示
- 保存操作
- Undo / Redo操作
- ユーザー操作管理

---

## 2.2 PDF編集エンジン

Python + PyMuPDFを使用する。

### 役割

PyMuPDFでは以下を担当する。

- PDFファイル読み込み
- PDF内部の文字情報取得
- テキスト座標取得
- フォントサイズ取得
- フォント情報取得
- 文字色取得
- 元テキスト削除
- 編集後テキスト挿入
- PDF保存

---

## 2.3 TauriとPythonの連携

TauriからPythonプロセスを呼び出す。

基本構成は以下とする。

```text
React
  ↓
Tauri
  ↓
Rust
  ↓
Python
  ↓
PyMuPDF
  ↓
PDFファイル
```

Python側はPDF編集エンジンとして独立させる。

ReactからPyMuPDFを直接呼び出さない。

---

# 3. 対応OS

## 3.1 初期対応

macOS

## 3.2 将来的な対応

以下への対応を考慮した設計とする。

- Windows
- Linux

ただし初期バージョンではmacOSのみ動作保証対象とする。

---

# 4. 対象PDF

## 4.1 対応対象

以下のPDFを対象とする。

- テキスト情報を保持しているPDF
- パソコン上で作成されたPDF
- Word、Excel、ブラウザ等から生成されたPDF

---

## 4.2 初期バージョンで対象外とするPDF

以下は初期バージョンでは対象外とする。

- スキャンPDF
- 画像のみで構成されたPDF
- パスワード保護されたPDF
- DRM等で編集制限されたPDF
- 電子署名済みPDFの署名維持

スキャンPDFについては将来的にOCR対応を検討する。

---

# 5. 基本画面

画面構成は以下とする。

```text
┌──────────────────────────────────────────────┐
│ 開く │ 保存 │ Undo │ Redo │ - │ 100% │ + │
├────────────┬─────────────────────────────────┤
│            │                                 │
│ Page 1     │                                 │
│ ┌────────┐ │                                 │
│ │        │ │          PDF表示                │
│ └────────┘ │                                 │
│            │     文章をクリックして編集      │
│ Page 2     │                                 │
│ ┌────────┐ │                                 │
│ │        │ │                                 │
│ └────────┘ │                                 │
│            │                                 │
├────────────┴─────────────────────────────────┤
│ ページ 1 / 5                ズーム 100%      │
└──────────────────────────────────────────────┘
```

---

# 6. PDF読み込み機能

## 6.1 PDFファイル選択

ユーザーは「開く」ボタンからPDFファイルを選択できる。

対応拡張子：

```text
.pdf
```

---

## 6.2 ドラッグ&ドロップ

PDFファイルをアプリケーションへドラッグ&ドロップすることで開ける。

---

## 6.3 読み込み処理

PDF読み込み時にPython側で以下を解析する。

- ページ数
- ページサイズ
- テキスト
- テキスト座標
- フォントサイズ
- フォント名
- 文字色

---

# 7. PDF表示機能

PDF.jsを使用してPDFを表示する。

以下を実装する。

- ページ表示
- スクロール
- ページ切り替え
- ズームイン
- ズームアウト
- 100%表示
- ウィンドウ幅に合わせる

---

# 8. ページサムネイル

画面左側にPDFページのサムネイルを表示する。

サムネイルをクリックすると対象ページへ移動する。

---

# 9. テキスト解析

PyMuPDFを使用してPDF内テキストを解析する。

各テキストについて以下を取得する。

```text
page
text
x0
y0
x1
y1
font
fontSize
color
```

例：

```json
{
  "page": 1,
  "text": "契約期間は2026年3月31日までとする。",
  "x0": 120.5,
  "y0": 280.2,
  "x1": 380.5,
  "y1": 300.5,
  "font": "NotoSansJP-Regular",
  "fontSize": 12,
  "color": "#000000"
}
```

---

# 10. テキスト選択機能

PDF上の文章をクリックすると編集対象として選択する。

選択中のテキスト領域を枠で表示する。

例：

```text
契約期間は

┌──────────────────────────┐
│ 2026年3月31日までとする。 │
└──────────────────────────┘
```

---

# 11. テキスト編集機能

## 11.1 編集開始

テキストを選択すると、選択した位置に編集用入力欄を表示する。

```text
┌──────────────────────────┐
│ 2026年3月31日までとする。 │
└──────────────────────────┘
```

ユーザーは通常のテキストエディタと同じ感覚で文章を書き換えられる。

---

## 11.2 編集確定

以下の操作で編集を確定する。

- Enter
- 編集領域外クリック
- 確定ボタン

---

## 11.3 編集キャンセル

Escapeキーで編集をキャンセルできる。

---

# 12. PDFテキスト置換

文章編集時は以下の処理を行う。

```text
編集対象テキスト選択

        ↓

元テキスト座標取得

        ↓

PyMuPDF Redaction設定

        ↓

元テキスト削除

        ↓

編集後テキスト挿入

        ↓

PDF再描画

        ↓

画面更新
```

---

# 13. 文字削除処理

元テキストはPyMuPDFのRedaction機能を利用して削除する。

単純に白色矩形を上に重ねるだけではなく、可能な限り元のテキスト情報を削除する。

---

# 14. 文字再配置

編集後のテキストを元のテキスト位置へ配置する。

可能な限り以下の情報を維持する。

- 座標
- フォントサイズ
- 文字色
- 行間
- テキスト配置

---

# 15. フォント処理

PDF内部で使用されているフォント情報を取得する。

可能であれば同じフォントを使用して文字を再描画する。

同じフォントを使用できない場合は代替フォントを使用する。

日本語については日本語表示可能なフォントを使用する。

---

# 16. 日本語対応

以下を正常に編集できることを目標とする。

```text
ひらがな
カタカナ
漢字
英数字
記号
```

例：

```text
変更前

契約期間は2026年3月31日までとする。

変更後

契約期間は2027年3月31日までとする。
```

---

# 17. 編集領域制約

初期バージョンでは、編集後の文章は元のテキスト領域内に収めることを基本とする。

例えば、

```text
変更前

契約日は2026年4月1日です。
```

から、

```text
変更後

本契約における正式な契約開始日は2026年4月1日です。
```

のように大幅に文章が長くなる場合は、自動レイアウト調整を保証しない。

---

# 18. オーバーフロー処理

編集後テキストが元の領域を超えた場合、ユーザーへ警告する。

例：

```text
文章が編集可能領域を超えています。

・文字を短くしてください
・文字サイズを小さくしてください
```

---

# 19. Undo機能

直前の編集を取り消せる。

ショートカット：

```text
Command + Z
```

---

# 20. Redo機能

Undoした編集を再実行できる。

ショートカット：

```text
Command + Shift + Z
```

---

# 21. 編集履歴

編集操作を内部的に履歴として保持する。

例：

```json
{
  "page": 1,
  "oldText": "2026年3月31日",
  "newText": "2027年3月31日",
  "rect": {
    "x0": 100,
    "y0": 200,
    "x1": 200,
    "y1": 220
  }
}
```

---

# 22. 保存機能

## 22.1 上書き保存

現在開いているPDFへ上書き保存する。

ショートカット：

```text
Command + S
```

---

## 22.2 名前を付けて保存

別ファイルとして保存できる。

ショートカット：

```text
Command + Shift + S
```

---

# 23. 自動バックアップ

編集開始時に元PDFの一時バックアップを作成する。

編集失敗時に元PDFを破損させない設計とする。

---

# 24. エラー処理

以下の場合にはユーザーへエラーを表示する。

- PDF読み込み失敗
- PDF解析失敗
- PDF保存失敗
- フォント読み込み失敗
- テキスト置換失敗
- Pythonプロセス異常終了

---

# 25. Pythonインターフェース

Python側はCLIまたはJSONベースのインターフェースとして設計する。

例：

```text
pdf_engine
```

---

## 25.1 テキスト解析

入力：

```json
{
  "action": "extract_text",
  "file": "/path/sample.pdf"
}
```

出力：

```json
{
  "pages": [
    {
      "page": 1,
      "texts": []
    }
  ]
}
```

---

## 25.2 テキスト置換

入力：

```json
{
  "action": "replace_text",
  "file": "/path/sample.pdf",
  "page": 1,
  "rect": {
    "x0": 100,
    "y0": 200,
    "x1": 300,
    "y1": 220
  },
  "oldText": "2026年3月31日",
  "newText": "2027年3月31日"
}
```

---

# 26. Rust側責務

Rust側では以下を担当する。

- ファイルダイアログ
- ファイルパス管理
- Pythonプロセス起動
- PythonとのJSON通信
- エラーハンドリング
- 一時ファイル管理
- 保存処理管理
- OS関連処理

PDF編集ロジックをRust側へ直接実装しない。

---

# 27. React側責務

React側は以下を担当する。

- UI
- PDF.js表示
- テキスト選択
- 編集UI
- 入力制御
- Undo / Redo
- 編集状態管理
- サムネイル
- ズーム

---

# 28. セキュリティ

PDF編集はローカル環境のみで実行する。

PDFを外部サーバーへ送信しない。

ユーザーのファイルは端末内のみで処理する。

---

# 29. 非機能要件

## 29.1 パフォーマンス

一般的なPDFについて、操作時に大きなストレスを感じない速度を目標とする。

目安：

```text
100ページ程度
ファイルサイズ50MB程度
```

---

## 29.2 安定性

編集中にアプリが異常終了しても元PDFを破損させない。

---

## 29.3 UI

Macアプリとして違和感の少ないデザインとする。

---

# 30. 初期バージョン MVP

Version 1では以下のみ実装する。

1. PDFを開く
2. PDFを表示する
3. ページ移動
4. ズーム
5. PDFテキスト情報取得
6. テキスト選択
7. テキスト編集
8. 元テキスト削除
9. 新テキスト挿入
10. Undo
11. Redo
12. PDF保存
13. 名前を付けて保存

---

# 31. Version 2

以下を追加する。

- フォント変更
- フォントサイズ変更
- 文字色変更
- 太字
- 文字位置微調整
- 複数行テキスト編集
- ページ削除
- ページ並び替え
- PDF結合
- PDF分割

---

# 32. Version 3

以下を検討する。

- OCR
- スキャンPDF編集
- 画像挿入
- 画像削除
- ハイライト
- 図形挿入
- 電子署名
- コメント
- 注釈
- PDFフォーム編集

---

# 33. 将来的な高度編集

最終的には以下のような編集を検討する。

```text
文章を長くする
       ↓
行を自動折り返し
       ↓
下の文章を自動移動
       ↓
ページ全体を再レイアウト
```

ただしこれはPDFの構造上非常に複雑になるため、初期バージョンの対象外とする。

---

# 34. システム構成

```text
┌────────────────────────────────────┐
│              Tauri                 │
│                                    │
│ ┌────────────────────────────────┐ │
│ │ React + TypeScript             │ │
│ │                                │ │
│ │ PDF.js                         │ │
│ │ PDF Viewer                     │ │
│ │ Text Editor                    │ │
│ │ Thumbnail                      │ │
│ │ Undo / Redo                    │ │
│ └───────────────┬────────────────┘ │
│                 │                  │
│              invoke               │
│                 │                  │
│ ┌───────────────▼────────────────┐ │
│ │ Rust                           │ │
│ │                                │ │
│ │ File Management                │ │
│ │ Python Process Management      │ │
│ │ JSON Communication             │ │
│ └───────────────┬────────────────┘ │
└─────────────────┼──────────────────┘
                  │
                  ▼
        ┌────────────────────┐
        │ Python             │
        │                    │
        │ PyMuPDF            │
        │                    │
        │ Text Extraction    │
        │ Text Redaction     │
        │ Text Insertion     │
        │ PDF Save           │
        └─────────┬──────────┘
                  │
                  ▼
              PDF File
```

---

# 35. 開発方針

最初からAdobe Acrobatと同等の機能を目指さない。

まず以下を最優先とする。

> PDF内の既存文章をクリックし、その文章を書き換えて保存できる。

この一連の機能を安定して実現した後に、PDF編集機能を段階的に拡張する。

# 36.推奨ディレクトリ構成

pdf-editor/
│
├── src/
│   ├── components/
│   │   ├── PdfViewer/
│   │   ├── PdfToolbar/
│   │   ├── PdfThumbnail/
│   │   └── TextEditor/
│   │
│   ├── hooks/
│   │
│   ├── types/
│   │
│   ├── services/
│   │   └── pdfEngine.ts
│   │
│   ├── App.tsx
│   └── main.tsx
│
├── src-tauri/
│   ├── src/
│   │   ├── commands/
│   │   │   └── pdf.rs
│   │   │
│   │   ├── lib.rs
│   │   └── main.rs
│   │
│   ├── Cargo.toml
│   └── tauri.conf.json
│
├── python/
│   ├── .venv/
│   ├── pdf_engine.py
│   │
│   └── services/
│       ├── extract_text.py
│       ├── replace_text.py
│       └── save_pdf.py
│
├── package.json
└── README.md
